# Context budget

Before loading evidence ask whether it changes the next decision. Start with
the manifest, then a targeted slice. Do not load full terminal, CI or history
when a bounded structured result is sufficient.
