# Dependencies
node_modules/
# Sem a barra tambem: `node_modules/` casa so com diretorio, e um SYMLINK
# chamado node_modules (worktree apontando para a instalacao principal) passa
# batido e vai parar no commit. Aconteceu no PR #83.
node_modules
.pnp
.pnp.js

# Build artifacts
.next/
out/
dist/
build/
.turbo/
.vercel/

# Env — ignora todo .env*, exceto os templates de exemplo
.env*
!.env.example
!.env.hostgator.example

# Testing & coverage
coverage/
.nyc_output/
playwright-report/
test-results/
e2e-artifacts/

# IDE / OS
.DS_Store
.vscode/*
!.vscode/extensions.json
!.vscode/settings.recommended.json
.idea/

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# TypeScript
*.tsbuildinfo
next-env.d.ts

# Python (skill scripts, etc)
__pycache__/
*.pyc

# Supabase local
infra/supabase/.temp/
infra/supabase/.branches/

# Epic executor (skill artifacts)
.epic-executor/

# Obsidian knowledge export artifacts (scripts/obsidian-export.ts) — sanitized
# but still pending human review/upload via the CRM knowledge UI; never
# committed, same reasoning as the rest of this file's generated/local dirs.
.local/

# Claude Code harness artifacts (scheduled tasks lock, settings locais, etc)
# gov-loop: agents/ e commands/ são maquinaria versionada do loop.
# rules/, skills/, agents/, commands/ e settings.json são compartilhados.
# settings.local.json e o restante de .claude/ são locais.
.claude/
!.claude/
.claude/*
!.claude/agents/
!.claude/commands/
!.claude/skills/
!.claude/skills/**
!.claude/rules/
!.claude/rules/**
!.claude/settings.json
.claude/settings.local.json

# Local docker volumes (WAHA)
.waha-data/
.epic-executor/
.playwright-mcp/

# Sentry Config File
.env.sentry-build-plugin
inbox-with-real-msgs.png
.e2e-creds.json
.e2e-agente-mcp.json
test-results/
playwright-report/
.vercel
deskcommcrm-setup-kit.zip

# gov-loop — estado operacional fica FORA do git (a resposta do humano na inbox
# e a linha 'started' do sessions.log não podem sujar o working tree; a guarda de
# chão limpo do LOOP.md os stasharia). tooling/agent-loop/checkpoints/evidence/ NÃO é ignorado:
# screenshot de checkpoint é evidência versionada.
tooling/agent-loop/inbox.items.md
tooling/agent-loop/sessions.log
tooling/agent-loop/logs/
tooling/agent-loop/locks/
tooling/agent-loop/STOP
tooling/agent-loop/run-session.sh
.superpowers/
.worktrees/

# graphify (análise local de knowledge graph)
graphify-out/
package-lock.json
.e2e-invite.json
.e2e-owner.json
.e2e-invite-url.json
.superpowers/evidence/
evidence/.sonda-score-residuo.json

# sessão reusada pelas jornadas (contém tokens)
.auth/
.pnpm-store/
tooling/scripts/lead-pipeline/bin/

# Maestri (config local da estação, não é doutrina compartilhada)
.maestri/

# caches locais de pesquisa (SQLite gerado, não é fonte)
docs/pesquisa/**/*.db

# Artefactos locais de sessões de pesquisa lead-pipeline (não são fonte versionada)
docs/pesquisa/lead-pipeline-run-2026-09-02-sion/
docs/pesquisa/lead-pipeline-run-2026-09-03-sion-ptbr/
CONTEXTO-ALICERCE.md
