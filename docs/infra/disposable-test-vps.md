# Disposable Test VPS

## Canonical repository

- Repository: `https://github.com/trydavidqix/Lumenva`
- Visibility: public.
- Legacy repository: `trydavidqix/Lumenva-Legacy`, private archive.
- Self-hosted runner: `lumenva-disposable-test-vps`.
- Required labels: `self-hosted`, `Linux`, `X64`, `self-hosted-lumenva-disposable`.

Runner registration tokens are repository-scoped. After repository migration, the VPS runner was removed from the Legacy repository and registered against the canonical repository. Verify runner status before relying on GitHub Actions.

Hosted GitHub Actions remains an available alternative because the canonical repository is public. Current workflows remain configured for the disposable self-hosted runner.

