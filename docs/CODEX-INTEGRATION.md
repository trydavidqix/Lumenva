# Codex integration

The reproducible global Executor constitution is in
`config/agent-orchestration/codex`. Install it only after backing up the
machine-managed Codex files. The global agent is intentionally short and
delegates detailed evidence handling to the MCG runtime.
