# Handoff F6/F7/F8 — estado em 2026-09-24 (21:40 GMT)

> Snapshot temporal. Doutrina duradoura vive em `CLAUDE.md`, `.claude/rules/` e specs. Revalide antes de confiar nos números.

## Pipeline

20/25 tarefas concluídas (80%). Restam: F7-C1 e F8-C1 (re-revisões de segurança) e os três H1 do Owner (F6-H1 ativação do Drizzle, F7-H1 sandboxes/credenciais/custo, F8-H1 console GCP e recursos reais, incluindo restringir os claims do WIF ao repositório).

## Já na `main`

- #56–#64: F6-J2/J3/J4, F8-J6 runbook, `AGENTS.md` (TDD), F7-J6 matriz de adapters, correções F6-C1, F8-C1 (scheduler com sha256 + `timingSafeEqual`, job `gcp-auth`), fiação dos pacotes ao workspace.
- #65: correções F7 (WAHA aviso sem assinatura, Meta rejeita token ausente, SSRF Nuvemshop no pacote, Stripe).
- #67: scanners report-only (Semgrep, Gitleaks, OSV-Scanner) em `security-scan.yml`; #70: decisões de scanning (ZAP adiado, sem Docker).
- #68: bootstrap-owner sem log sensível, 2 regex sem backtracking (R2 e brightbean), `env-alias` sem injeção de shell, `permissions: contents: read` nos workflows.

## Em aberto

- **#69 (Lote D)**: `test:unit` em 14 pacotes. CI com `--no-bail` no script raiz mostra falhas antes escondidas: `engineering-core` (4 testes: `allowed-gates`, `cli-exit` x3, causa a confirmar), `social-brain-mcp` (`server.test.ts`: espera 12 tools, servidor expõe 21), e `social-brain/db` `r2.test.ts` (teste do #68 falha com `authorization_denied`: corrigir o TESTE com actor válido, sem tocar a implementação). Caminhos de teste de `operating-core`, `tenant-rls` e `foundation-coverage` já corrigidos. Pendente responder: por que `integrations/meta` e `stripe` ficaram sem `typecheck`. Não mergear com falha nova sem decisão do Owner.
- **#66** Nuvemshop webhooks falham fechado: parado de propósito (Nuvemshop no fim da fila).
- **F7-C1**: re-revisão reprovou por 1 MEDIUM novo: `apps/crm/app/api/v1/stripe/webhook/route.ts` lê o corpo inteiro (`await req.text()`) antes de validar a assinatura, sem limite de bytes. Correção: limite (413) por Content-Length e por leitura em streaming, depois validar assinatura; TDD. O HIGH do WAHA está mitigado; o suposto HIGH da Nuvemshop foi retirado (URLs são constantes oficiais).
- **F8-C1**: re-revisão interrompida por limite de uso; refazer read-only na `main`.
- **Jules Lote A** (dependências, Next.js ≥16.3.3, vitest ≥4.1.11, pipecat ≥1.4.0): plano gerado, dentro da allowlist, sem PR ainda.
- Dependabot: ~45 alertas abertos; security updates (PRs automáticos) desligados aguardando decisão do Owner.
- CodeQL: alertas restantes a reconferir após o #68.

## Decisões do Owner (2026-09-24)

- Testes pesados rodam no CI (GitHub Actions), não local.
- WAHA: default do código não muda; novas instalações com `WAHA_WEBHOOK_REQUIRE_SIGNATURE=true`.
- Nuvemshop congelada e por último; substituto europeu a escolher.
- ZAP adiado: sem Docker, só tarball Java, só com ambiente de teste (`.env.e2e`); nunca produção.
- Modelo Codex fixo em `gpt-5.6-sol` esforço `medium`. Conferir o rodapé de cada terminal Codex antes de delegar; a cota de 5h é da conta e é compartilhada.

## Estado dos agentes ao encerrar

Codex CTO e Farol (Security Tooling Engineer) sem cota até 25/09 02:13. Sem trabalho pendente só local: o que existe está em PRs no GitHub ou nesta doc.

## Próximos passos

1. Conferir modelo/esforço dos Codex; retomar após 02:13.
2. Fechar #69: consertar o teste R2, analisar `engineering-core` e MCP, decidir sobre falhas remanescentes.
3. PR do limite de corpo do Stripe; depois re-revisão F7-C1.
4. Re-revisão F8-C1.
5. Jules Lote A; conferir queda dos alertas do Dependabot.
6. Owner: H1s, e-commerce europeu substituto, Dependabot security updates, teste de login com `.env.e2e`.
