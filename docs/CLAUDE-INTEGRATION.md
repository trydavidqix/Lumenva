# Claude integration

The reproducible CEO constitution, rules and output style are in
`config/agent-orchestration/claude`. Existing global Claude configuration must
be merged additively; managed auth/cache files are never rewritten.
