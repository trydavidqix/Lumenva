# F4-SENTIMENT-001 — spec/DoD

## Entrada e limites

- Base: `57d86505da06129ade1f0a0c457d38f1829f6e43` (F4-RAG + F4-HANDOFF).
- Worktree: `/home/claude/src/worktrees/f4-sentiment-001-2026-09-10`.
- Implementar apenas um workflow de sentimento Agent OS local, determinístico e sem provider, credenciais, rede, migrations ou alterações ao schema canónico.
- Não tocar `main`.

## Inventário da fonte (`path:line`)

- `apps/crm/workers/ai-sentiment-worker.ts:55-59` — contrato atual `SentimentResult`.
- `apps/crm/workers/ai-sentiment-worker.ts:61-107` — guards e dependência de gateway/DB do worker existente.
- `apps/crm/workers/ai-sentiment-worker.ts:126-170` — classificação atual via `generateObject`/provider.
- `apps/crm/workers/ai-sentiment-worker.ts:172-239` — persistência de metadata e emissão de alerta.
- `apps/crm/workers/ai-sentiment-worker.handler.ts:12-30` — contrato de dispatch do handler existente.
- `apps/crm/lib/ai/prompts/sentiment.ts:11-24` — escala semântica de sentimento 0..1.
- `apps/crm/lib/agent-engine/agent/intent-classifier.ts:21-24,57-83` — padrão de veredicto tipado e parsing fail-closed.
- `apps/crm/lib/agent-engine/agent/stage-classifier.ts:72-84,121-133` — sugestão determinística/volátil sem escrita de estado.
- `apps/crm/lib/agent-engine/agent/f4-rag-001.test.ts:8-25` — padrão de teste F4 e provider-free.
- `apps/crm/package.json:16-20,45-46` — lint/typecheck e test:fast existentes.

## Saída verificável / DoD

1. Novo módulo Agent OS expõe contrato tipado para classificar texto em `negative | neutral | positive`, score determinístico em `[0,1]` e sinais usados, sem imports de provider, credenciais, rede ou DB.
2. O mesmo input produz byte-for-byte o mesmo output; texto vazio e texto sem sinais degradam para neutro sem lançar.
3. O teste alvo cobre casos negativo, neutro e positivo, limites e repetição determinística.
4. `test:fast` inclui o teste de sentimento e mantém os testes F4-RAG/F4-HANDOFF.
5. `EXIT_TEST_FAST=0` e `EXIT_TYPECHECK=0`; lint é executado e qualquer dívida preexistente permanece documentada, sem correção em massa.
6. Commit Conventional imperativo com body o quê/porquê/impacto/testado e `Refs F4-SENTIMENT-001`.

## Riscos

- Não substituir o worker LLM existente nem alterar o caminho live: esta fatia prova apenas o workflow determinístico local do Agent OS.
- Léxico pequeno é conservador e não pretende ser modelo semântico; provider-backed classification permanece fora desta tarefa.
