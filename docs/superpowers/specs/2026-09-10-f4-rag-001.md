# F4 — Spec e DoD

## Entrada e limites

- Base: `mvp/crm-completo@c673a94a598c419b68366f6709a65846968b9fb1`.
- Execução: worktree isolada `f4/rag-001-2026-09-10`.
- Sem tocar `main`, schema canónico, migrations, providers novos, credenciais novas, deploy ou push.

## Fatias pequenas

1. **F4-RAG-001 (dispatchada):** provar retrieval e embeddings existentes, com teste determinístico e `test:fast`.
2. **F4-HANDOFF-001:** auditar/fechar detecção, persistência idempotente e silêncio após handoff em `human-handoff.ts`, sem alterar schema.
3. **F4-SENTIMENT-001:** auditar o classificador auxiliar pelo seam existente, fallback determinístico e budget bounded.
4. **F4-BUDGET-001:** auditar limites de contexto/índice e rejeição explícita de excesso usando contadores existentes.
5. **F4-INTEGRATION-001:** juntar os testes alvo e gates, sem provider externo ou coverage.

## F4-RAG-001 — contrato exato

### Inventário path:line

- `apps/crm/lib/agent-engine/agent/search-knowledge.ts:33-139`: `searchKnowledge`, embedding, RPC `retrieve_top_k_chunks`, threshold, telemetria e fallback `knowledge_unavailable`.
- `apps/crm/lib/agent-engine/agent/search-knowledge.ts:155-163`: conversão de hits em citações.
- `apps/crm/lib/agent-engine/edge/llm/embed.ts:21-109`: contrato de embedding de 1536 dimensões, allowlist e erro fail-closed.
- `apps/crm/lib/agent-engine/edge/llm/embed.ts:40-55`: configuração existente por ambiente; não criar chave ou provider.
- `apps/crm/lib/agent-engine/agent/search-knowledge.test.ts:1-307`: testes existentes de RPC, threshold, top score, NaN, telemetria e citações.
- `apps/crm/lib/agent-engine/agent/inbound-turn.ts:524-607`: seam de retrieval opcional já usado no turno.
- `apps/crm/lib/agent-engine/env.ts:138-144`: knobs RAG existentes e opcionais.
- `apps/crm/package.json:16-45`: scripts existentes; adicionar somente `test:fast` se não existir, limitado ao teste determinístico RAG.

### Implementação mínima

- Reutilizar `searchKnowledge` e `embedQuery`/`embedText` existentes.
- Não chamar rede no teste: injetar embedding determinístico e pool/RPC fake já no padrão dos testes existentes.
- Cobrir uma busca com hit acima do threshold, uma sem hit e erro de embedding sem exceção não tratada.
- `test:fast` deve executar apenas o teste alvo RAG, sem coverage.

### DoD verificável

- [ ] `searchKnowledge` devolve hits ordenados/filtrados pelo threshold e citações no shape existente.
- [ ] O vetor usado no RPC mantém o contrato de 1536 dimensões; o teste não usa provider real.
- [ ] Falha de embedding/RPC retorna `knowledge_unavailable` sem vazar query, chave ou credencial.
- [ ] Teste determinístico passa duas vezes com os mesmos inputs.
- [ ] `pnpm --filter lumenva-crm test:fast` termina com `EXIT_TEST_FAST=0` e sem coverage.
- [ ] Gates alvo `typecheck` e `lint` terminam com exit 0.
- [ ] Commit Conventional Commit imperativo, body com o quê/por quê/impacto/testado e `Refs F4-RAG-001`.

## Riscos e gates

- Risco: embeddings reais dependem de chave e rede; por isso ficam fora do teste local e do CI.
- Risco: alterações de `searchKnowledge` podem afetar `inbound-turn`; executar o teste existente dessa integração antes de fechar.
- Risco: `top_score`/NaN e tenant filter são contratos de segurança; não simplificar os asserts existentes.
- Gate: `git diff --check`, teste alvo RAG, `test:fast`, `typecheck`, `lint`; sem `db:migrate`, migrations, schema canónico, `main`, deploy ou provider novo.
