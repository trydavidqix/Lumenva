# F4-HANDOFF-001 — Spec e DoD

## Entrada e limites

- Base: `d03fe9606e2afee3525912fbe378f3ff7be0d510` (F4-RAG-001 aprovado local condicionalmente).
- Worktree: `f4/handoff-001-2026-09-10`.
- Não tocar `main`, schema canónico, migrations, providers, credenciais, deploy ou transporte externo.

## Inventário path:line

- `apps/crm/lib/agent-engine/agent/human-handoff.ts:55-69`: padrões e `detectHumanHandoffRequest`.
- `apps/crm/lib/agent-engine/agent/human-handoff.ts:112-142`: `detectAmbiguousOptOut` e `isLeadInHandoff`.
- `apps/crm/lib/agent-engine/agent/human-handoff.ts:145-269`: `HandoffIds` e `performHumanHandoff` (force_human, silêncio, cancelamento, inbox, caso formal e timeline).
- `apps/crm/lib/agent-engine/agent/human-handoff.ts:271-323`: schema estrito e `applyRequestHumanHandoff`.
- `apps/crm/lib/agent-engine/agent/human-handoff.ts:329-360`: `buildHandoffSummary`.
- `apps/crm/tests/invariants/gov-6-ai-handoff.test.ts`: contrato de isolamento/persistência existente.
- `apps/crm/tests/unit/escalacao-retomada.test.ts:405-430`: regressão do caso formal aberto pelo handoff.
- `apps/crm/tests/unit/declaracao-do-turno.test.ts:127-180`: contrato determinístico do resumo.
- `apps/crm/package.json:16-45`: scripts; `test:fast` deve incluir apenas o teste alvo desta fatia.

## Implementação mínima

- Preservar o contrato de produção já existente: ids vêm do runtime, payload aceita apenas `reason`, `force_human` é irrevogável pelo agente, silêncio é durável e reexecução é idempotente.
- Adicionar teste determinístico sem rede/provider que execute `applyRequestHumanHandoff` com pool fake roteado por SQL, verificando as mutações essenciais e repetição sem duplicar inbox/caso.
- Cobrir payload extra recusado e `buildHandoffSummary` estável; não alterar schema nem adicionar colunas.
- Adicionar/ajustar `test:fast` para executar somente esse teste alvo.

## DoD

- [ ] Pedido válido cria handoff com `force_human`, silêncio, cancelamento, inbox e caso/timeline através das funções existentes.
- [ ] Segunda execução no mesmo episódio não duplica item aberto nem caso.
- [ ] Payload com campo não permitido retorna `invalid_payload` sem tocar banco.
- [ ] Teste determinístico passa com os mesmos inputs sem provider/rede.
- [ ] `pnpm --filter lumenva-crm test:fast` termina `EXIT_TEST_FAST=0`.
- [ ] `pnpm --filter lumenva-crm typecheck` termina `EXIT_TYPECHECK=0`.
- [ ] `pnpm --filter lumenva-crm lint` deve ser reportado com exit real; dívida preexistente fica fora desta fatia.
- [ ] Commit Conventional imperativo com body o quê/porquê/impacto/testado e `Refs F4-HANDOFF-001`.
