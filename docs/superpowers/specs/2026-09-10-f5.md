# F5 — plano e DoD inicial

## Fonte canónica

`project_plano_crm_completo_2026-09-09.md` não está presente no checkout em `831e794b`. Para esta execução, a fonte recuperada é a documentação canónica versionada do Agent OS: `docs/architecture/agent-os/execution.md`, `docs/architecture/agent-os/policies.md` e `docs/architecture/agent-os/phase-5-verification.md`, complementada pelo contrato do produto em `docs/prd/05-prd-ai-rag-handoff.md`.

## Entrada e limites

- Entrada: `mvp/crm-completo@831e794b3d063a9e9fb49d29d9d5495cd2cc663b`.
- Worktree isolada: `/home/claude/src/worktrees/f5-autonomy-001-2026-09-10`.
- Não tocar `main`, produção, credenciais, providers externos, migrations ou schema canónico.
- Máximo: 2 ciclos implementação/revisão-correção por tarefa.

## F5 decomposta em tarefas

1. **F5-AUTONOMY-001** — workflow determinístico que combina avaliação de evidência e autorização de promoção, preservando `SHADOW -> DRAFT -> ASSISTED`, ator humano obrigatório e bloqueio de autopilot.
2. **F5-APPROVAL-001** — auditar e cobrir contrato de aprovação durável e resume com idempotência estável.
3. **F5-KILLSWITCH-001** — auditar e cobrir reavaliação de kill-switch antes de efeitos colaterais.
4. **F5-EVIDENCE-001** — consolidar evidência/telemetria de decisão sem PII e gate local reproduzível.

## Inventário `path:line` da fonte

- `docs/architecture/agent-os/execution.md:25-39` — estados canónicos e transições determinísticas.
- `docs/architecture/agent-os/execution.md:41-68` — limites, repetição, idempotência.
- `docs/architecture/agent-os/execution.md:74-89` — semântica F5 SHADOW/DRAFT/ASSISTED, approval, kill-switch e limites.
- `docs/architecture/agent-os/policies.md:45-68` — kill switches, aprovação durável e progressão de autonomia.
- `apps/crm/lib/agent-engine/autonomy/promotion.ts:1-108` — evidência, thresholds e autorização de promoção existentes.
- `apps/crm/lib/agent-engine/autonomy/decision.ts:23-60` — resolução efetiva e precedência de kill-switch.
- `apps/crm/lib/agent-engine/contracts/autonomy-phase-5-integration.test.ts:58-184` — cobertura existente dos contratos F5.
- `apps/crm/package.json:16-20,45-46` — gates lint/typecheck/test:fast.

## DoD da primeira tarefa

- `F5-AUTONOMY-001` expõe um workflow puro, tipado e determinístico que avalia evidência e autoriza no máximo um avanço de nível.
- Ausência/evidência stale, thresholds violados, ator `model`, salto de nível e `autopilot_*` resultam em deny explícito.
- O workflow não importa rede/provider/credenciais/DB e não escreve estado.
- Teste alvo cobre allow e os principais denies, repetição determinística e fronteira `ASSISTED`.
- `test:fast` inclui o teste F5 e regressões F4; `EXIT_TEST_FAST=0`, `EXIT_TYPECHECK=0`; lint é executado e dívida baseline é documentada.
- Commit Conventional imperativo com body o quê/porquê/impacto/testado e `Refs F5-AUTONOMY-001`.

## Riscos

- A documentação declara F5 histórica como implementação completa, mas a execução atual precisa ser revalidada no SHA corrente.
- A composição nova não promove cliente nem altera runtime live; continua dependente de evidência fornecida pelo chamador.
