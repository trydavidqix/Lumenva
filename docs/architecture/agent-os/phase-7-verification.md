# Agent OS Phase 7 — Verification

## Decision

`INCOMPLETE`

The benchmark implementation is present and the current-engine/Inngest real local benchmark paths have demonstrated complete passing suites. Vercel Workflow has not yet produced a fresh post-fix passing comparative result, so no provider adoption decision is authorized.

## Latest verification evidence — 2026-08-19

Latest focused post-fix evidence supplied from the local Windows workspace:

- `pnpm exec vitest run tests/unit/durable-benchmark-vercel-workflow-proxy-matcher.test.ts`: **1/1 file, 2/2 tests PASS**;
- proxy contract proves Workflow internal Local World requests bypass the Next proxy matcher while ordinary application/API routes remain behind it;
- `pnpm phase7:benchmark:all` focused test stage: **15/15 files, 34/34 tests PASS**;
- the same command then stopped during `pnpm typecheck` on malformed generated Next development types in `.next/dev/types/routes.d.ts`;
- because the command stopped before `scripts/phase7-benchmark-all.ts`, no new three-engine runtime decision was generated by this latest attempt.

The malformed `.next/dev/types/routes.d.ts` is generated development output, not committed benchmark source. It must be regenerated cleanly before interpreting another typecheck result.

## Last complete comparative runtime result before the generated-type interruption

| Engine | Runs | Hard gates | Status |
|---|---:|---|---|
| current | 208/208 | PASS | PASS |
| Inngest | 208/208 | PASS | PASS |
| Vercel Workflow | 208/208 | FAIL | FAIL |

Decision from that complete run: `INCOMPLETE`.

The focused proxy fix is GREEN, but it does not by itself prove that Vercel Workflow now passes the full benchmark. Only a fresh complete comparative run may replace the table above.

## Benchmark contract

All three engines are evaluated against the same eight versioned synthetic scenarios and small/medium/stress profiles. Each engine executes **208 total runs**. Hard gates are evaluated before scoring. Synthetic organizations/effects only are permitted.

Historical customer volume remains not applicable for this new-product synthetic runtime comparison and must not be fabricated. This benchmark does not claim live SHADOW/ASSISTED customer performance.

## Current gate table

| Gate | Status |
|---|---|
| repository/branch isolation | PASS |
| production/customer safety boundary | PASS by benchmark design |
| focused Vercel Workflow proxy regression | PASS — 1/1 file, 2/2 tests |
| comparative focused tests | PASS — 15/15 files, 34/34 tests |
| latest TypeScript attempt | BLOCKED by malformed generated `.next/dev/types/routes.d.ts`; requires clean regeneration |
| current-engine real local benchmark | PASS — 208/208; hard gates PASS in last complete comparative run |
| Inngest real local benchmark | PASS — 208/208; hard gates PASS in last complete comparative run |
| Vercel Workflow real local benchmark | FAIL — 208/208; hard gates FAIL in last complete comparative run; post-fix rerun pending |
| cross-engine final comparison | INCOMPLETE |
| weighted final recommendation | INCOMPLETE |

## Safety and evidence hygiene

No production migration, customer message, billing change, secret commit, remote migration, GitHub Actions run, or Vercel Preview deployment is authorized. Benchmark organizations and effects remain synthetic. Provider payloads/secrets/PII must not be committed as evidence.

## Exit criteria

Phase 7 may leave `INCOMPLETE` only after a clean post-fix `pnpm phase7:benchmark:all` completes end-to-end on an exact code SHA and reports all three engines. Typecheck must pass in that same evidence chain. Hard gates must pass before any provider is eligible for scoring/adoption.

Until then, no migration is authorized.
