; Extracted from the real DeskcommCRM production VPS (lumenva-crm), 2026-08-28.
; ARI is bound to loopback only (see http.conf) — never expose ARI's HTTP port on a
; public interface without a reverse proxy + auth in front of it.

[general]
enabled=yes
pretty=yes

[testuser]
type=user
read_only=no
password=<REDACTED>
