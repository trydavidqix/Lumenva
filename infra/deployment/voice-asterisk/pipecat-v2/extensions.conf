[voicecore-test]
; RTP-only diagnostic; does not invoke Pipecat.
exten => 700,1,NoOp(Isolated RTP Record test)
 same => n,Answer()
 same => n,Record(v2-test:wav,,30,k)
 same => n,Hangup()

; Full isolated Pipecat WebSocket path.
exten => 701,1,NoOp(Isolated Pipecat WebSocket test)
 same => n,Dial(WebSocket/pipecat-v2/c(slin16)f(json),60)
 same => n,Hangup()
