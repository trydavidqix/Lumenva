#!/usr/bin/env bash
# pre-commit — dispatcher dos guards de commit do gov-loop (DeskcommCRM).
# Armado via: git config core.hooksPath tooling/agent-loop/hooks
# Todos os guards precisam passar; qualquer um falhando bloqueia o commit.
set -euo pipefail
top="$(git rev-parse --show-toplevel)"
"$top/tooling/agent-loop/hooks/validate-features.sh"
"$top/tooling/agent-loop/hooks/check-migration-triple.sh"
"$top/tooling/agent-loop/hooks/freeze-invariants.sh"
