; Extracted from the real DeskcommCRM production VPS (lumenva-crm), 2026-08-28.
; ARI's HTTP/WebSocket server binds to loopback only — the voice-sip-worker on the same
; host talks to it over 127.0.0.1, nothing external reaches it directly.

[general]
enabled=yes
bindaddr=127.0.0.1
bindport=8088
