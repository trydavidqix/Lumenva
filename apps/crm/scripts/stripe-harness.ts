import { readFileSync } from "node:fs";
import { verifyStripeCatalog, type StripeCatalogExpectation } from "../lib/billing/stripe-cli-contract";
const [command, p, q] = process.argv.slice(2); if (command !== "verify-catalog" || !p || !q) process.exit(2);
const e: readonly StripeCatalogExpectation[] = [{ planSlug: "basic", productLookupKey: "lumenva_basic", priceLookupKey: "lumenva_basic_monthly_eur", amountCents: 2900, currency: "eur", interval: "month" }, { planSlug: "medium", productLookupKey: "lumenva_medium", priceLookupKey: "lumenva_medium_monthly_eur", amountCents: 7900, currency: "eur", interval: "month" }, { planSlug: "premium", productLookupKey: "lumenva_premium", priceLookupKey: "lumenva_premium_monthly_eur", amountCents: 19900, currency: "eur", interval: "month" }];
try { const r = verifyStripeCatalog(readFileSync(p, "utf8"), readFileSync(q, "utf8"), e); r.plans.forEach((x) => console.log(x.planSlug + ": Product " + (x.productFound ? "OK" : "MISSING") + ", Price " + (x.priceFound ? "OK" : "MISSING"))); if (!r.ok) { r.errors.forEach((x) => console.error("FAIL: " + x)); process.exit(1); } console.log("Stripe catalog verification PASS (test mode; CLI only)."); } catch (x) { console.error("FAIL: " + (x instanceof Error ? x.message : "invalid output")); process.exit(1); }
