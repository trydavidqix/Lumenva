import { describe, expect, it } from "vitest";
import { deliverBuild, type DeliveryArtifact, type DeliveryBuildEvidence, type DeliveryPlan } from "@/lib/product-factory/delivery";
import type { BuildPlanStateRow, BuildPlanStateStore } from "@/lib/product-factory/build-plan-state-store";
const plan:DeliveryPlan={delivery_plan_id:"d",organization_id:"o",project_id:"p",build_ref:"b",channels:["WEB_PREVIEW"],environment:"LOCAL",release_policy_version:"v",rollout:"NONE",support_owner:"s",acceptance_criteria:["ok"],status:"DRAFT"}; const artifact:DeliveryArtifact={delivery_artifact_id:"a",delivery_plan_id:"d",artifact_ref:"r",content_hash:"h",platform:"WEB",version:"1",provenance_refs:["p"],security_scan_refs:["s"],test_refs:["t"],status:"VERIFIED"}; const evidence:DeliveryBuildEvidence={build_ref:"b",organization_id:"o",project_id:"p",source_refs:["s"],test_refs:["t"],policy_version:"v",evidence_refs:["e"]};
function store():BuildPlanStateStore{return {get:async()=>undefined,recordAttempt:async()=>({id:"1",status:"RUNNING",attempts:1,blocked_at:null}),finish:async()=>undefined} as unknown as BuildPlanStateStore;}
describe("delivery execution boundary",()=>{it("não executa sem gate APPROVED/PACKAGED",async()=>{let ran=false;await expect(deliverBuild(plan,artifact,evidence,store(),async()=>{ran=true;})).rejects.toThrow("delivery plan must be APPROVED or PACKAGED");expect(ran).toBe(false)});it("rejeita plataforma incompatível antes de executar",async()=>{let ran=false;await expect(deliverBuild({...plan,status:"APPROVED",channels:["APP_STORE"]},{...artifact,platform:"WEB"},evidence,store(),async()=>{ran=true;})).rejects.toThrow("delivery_channel_platform_mismatch");expect(ran).toBe(false)});});
