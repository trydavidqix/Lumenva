import { expect, it } from "vitest";
import { publishContentItem } from "@/lib/content-os/distribution/publication-service";
it("service blocks stale provenance before job scheduling", async () => { let scheduled = false; const repo = { findContentItem: async () => ({ id: "item", organizationId: "org", status: "approved" }), findPublishGate: async () => ({ status: "passed" }), updateContentItem: async () => undefined, createPublicationJob: async () => { scheduled = true; throw new Error("must not schedule"); } }; await expect(publishContentItem(repo, { organizationId: "org", contentItemId: "item", connectionId: "c", idempotencyKey: "k", title: "t", body: {}, provenance: { contentId: "item", skill: "SEO", content: "x", source: "brief", freshness: "stale", confidence: 0.9, generatedAt: "2026-01-01T00:00:00Z" } })).rejects.toThrow("content_publication_blocked"); expect(scheduled).toBe(false); });
it("service blocks publication when provenance is omitted", async () => { let scheduled = false; const repo = { findContentItem: async () => ({ id: "item", organizationId: "org", status: "approved" }), findPublishGate: async () => ({ status: "passed" }), updateContentItem: async () => undefined, createPublicationJob: async () => { scheduled = true; throw new Error("must not schedule"); } }; await expect(publishContentItem(repo, { organizationId: "org", contentItemId: "item", connectionId: "c", idempotencyKey: "missing", title: "t", body: {} } as never)).rejects.toThrow("content_publication_blocked"); expect(scheduled).toBe(false); });
