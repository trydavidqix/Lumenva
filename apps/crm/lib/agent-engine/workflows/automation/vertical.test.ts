import { describe, expect, it } from "vitest"; import { automationSchedulingGraph } from "./graph"; describe("F2-F3 vertical automation", () => { it("returns stable seeded output", async () => { const input = { workflowRunId: "run-1", organizationId: "org-1", automationId: "auto-1", seed: "alpha", humanDecision: "approve" as const }; const a = await automationSchedulingGraph.invoke(input); const b = await automationSchedulingGraph.invoke(input); expect(a.output).toBeTruthy(); expect(a.output).toEqual(b.output); expect(a.status).toBe("completed"); }); });
