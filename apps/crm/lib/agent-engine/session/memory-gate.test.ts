import { describe, expect, it } from "vitest";
import { evaluateAuthenticatedMemoryGate, evaluateMemoryGate, type MemoryGatePolicy, type MemoryWrite } from "./memory-gate";
const write:MemoryWrite={owner:"agent-1",scope:"company:org-1",authority:3}; const policy:MemoryGatePolicy={owner:"agent-1",scope:"company:org-1",authority:2};
describe("MemoryGate",()=>{it("allows a write when owner, scope and authority satisfy policy",()=>expect(evaluateMemoryGate(write,policy)).toBe("ALLOW"));it("denies insufficient authority, owner mismatch and scope mismatch",()=>{expect(evaluateMemoryGate({...write,authority:1},policy)).toBe("DENY");expect(evaluateMemoryGate(write,{...policy,owner:"agent-2"})).toBe("DENY");expect(evaluateMemoryGate(write,{...policy,scope:"owner:david"})).toBe("DENY")});it("fails closed for missing or malformed authority policy",()=>{expect(evaluateMemoryGate(write,{...policy,authority:Number.NaN})).toBe("DENY");expect(evaluateMemoryGate({...write,authority:Number.POSITIVE_INFINITY},policy)).toBe("DENY");expect(evaluateMemoryGate(write,undefined as unknown as MemoryGatePolicy)).toBe("DENY")});it("requires authenticated requester tenant, actor and capability",()=>{const w={...write,organization_id:"org-1",actor_id:"agent-1"};const p={...policy,organization_id:"org-1",actor_id:"agent-1",capability:"memory.write"};expect(evaluateAuthenticatedMemoryGate(w,p,{organization_id:"org-1",actor_id:"agent-1",capabilities:["memory.write"]})).toBe("ALLOW");expect(evaluateAuthenticatedMemoryGate(w,p,{organization_id:"org-2",actor_id:"agent-1",capabilities:["memory.write"]})).toBe("DENY");expect(evaluateAuthenticatedMemoryGate(w,p,{organization_id:"org-1",actor_id:"forged",capabilities:["memory.write"]})).toBe("DENY");expect(evaluateAuthenticatedMemoryGate(w,p,{organization_id:"org-1",actor_id:"agent-1",capabilities:[]})).toBe("DENY");});});
