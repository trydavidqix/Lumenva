# Reduz o contexto de build e — crítico — mantém segredos fora da imagem.
# Sem isto o `next build` carregaria .env.local e embutiria/vazaria segredos.
node_modules
.next
.git
.env
.env.*
test-results
playwright.config.ts
*.png
*.log
.lina
docs
tasks
scratchpad
.vercel

# clone do source do mem0 (usado só pra buildar a imagem mem0 na VPS)
mem0-src
