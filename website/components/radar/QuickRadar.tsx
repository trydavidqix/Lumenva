import Link from "next/link"; import type { RadarQuickItem } from "@/lib/radar/types"; import styles from "./radar.module.css"; export function QuickRadar({items}:{items:readonly RadarQuickItem[]}){return <section className={styles.section}><p className={styles.eyebrow}>RADAR RÁPIDO</p><h2>Em poucas linhas</h2>{items.map(i=><article className={styles.quick} key={i.id}><time>{i.publishedAt}</time><div><h3>{i.href?<Link href={i.href}>{i.title}</Link>:i.title}</h3><p>{i.summary}</p></div></article>)}</section>}
