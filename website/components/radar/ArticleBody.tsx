import Image from "next/image"; import type { RadarBlock } from "@/lib/radar/types"; import styles from "./radar.module.css";
export function ArticleBody({blocks}:{blocks:RadarBlock[]}){return <div className={styles.body}>{blocks.map((b,i)=>{switch(b.type){case"paragraph":return <p key={i}>{b.text}</p>;case"heading":return b.level===2?<h2 key={i}>{b.text}</h2>:<h3 key={i}>{b.text}</h3>;case"bullets":return <ul key={i}>{b.items.map(x=><li key={x}>{x}</li>)}</ul>;case"quote":return <blockquote key={i}>{b.text}{b.cite&&<cite>{b.cite}</cite>}</blockquote>;case"callout":return <aside key={i} className={styles.callout}>{b.title&&<strong>{b.title}</strong>}<p>{b.text}</p></aside>;case"image":return <figure key={i}><Image src={b.src} alt={b.alt} width={1200} height={675}/>{b.caption&&<figcaption>{b.caption}</figcaption>}</figure>}})}</div>}
