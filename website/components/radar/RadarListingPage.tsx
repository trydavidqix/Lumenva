import type { RadarArticle } from "@/lib/radar/types"; import { ArticleList } from "./ArticleList"; import styles from "./radar.module.css"; export function RadarListingPage({title,description,articles}:{title:string;description:string;articles:RadarArticle[]}){return <main className={styles.shell}><header className={styles.hero}><p className={styles.eyebrow}>LUMENVA RADAR</p><h1>{title}</h1><p className={styles.lead}>{description}</p></header><ArticleList articles={articles}/></main>}
