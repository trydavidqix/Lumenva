import { expect,it } from "vitest"; import { getAllRadarArticles } from "@/lib/radar/articles"; import { serializeRadarRss } from "@/lib/radar/rss"; it("serializes an RSS channel with canonical article links",()=>{const xml=serializeRadarRss(getAllRadarArticles());expect(xml).toContain("<rss version=\"2.0\">");expect(xml).toContain("Lumenva Radar");expect(xml).toContain("/radar/")});
