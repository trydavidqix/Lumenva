import { expect,it } from "vitest"; import { getAllRadarArticles } from "@/lib/radar/articles"; import { getRelatedRadarArticles } from "@/lib/radar/related"; it("never returns the current article or duplicates",()=>{const a=getAllRadarArticles()[0]!;const related=getRelatedRadarArticles(a);expect(related.some(x=>x.slug===a.slug)).toBe(false);expect(new Set(related.map(x=>x.slug)).size).toBe(related.length)});
