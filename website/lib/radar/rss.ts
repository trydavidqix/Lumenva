import { getSiteUrl } from "@/lib/metadata"; import type { RadarArticle } from "./types";
const esc=(v:string)=>v.replace(/&/g,"&amp;").replace(/</g,"&lt;").replace(/>/g,"&gt;").replace(/\"/g,"&quot;");
export function serializeRadarRss(articles:RadarArticle[]){ const base=getSiteUrl(); const items=articles.map(a=>`<item><title>${esc(a.title)}</title><link>${new URL(`/radar/${a.slug}`,base)}</link><guid>${new URL(`/radar/${a.slug}`,base)}</guid><pubDate>${new Date(a.publishedAt+"T12:00:00Z").toUTCString()}</pubDate><description>${esc(a.excerpt)}</description></item>`).join(""); return `<?xml version="1.0" encoding="UTF-8"?><rss version="2.0"><channel><title>Lumenva Radar</title><link>${new URL("/radar",base)}</link><description>IA, agentes, automação e tecnologia aplicada.</description>${items}</channel></rss>`; }
