import { getSiteUrl } from "@/lib/metadata"; import type { RadarArticle } from "./types";
const esc=(v:string)=>v.replace(/&/g,"&amp;").replace(/</g,"&lt;").replace(/>/g,"&gt;").replace(/\"/g,"&quot;").replace(/'/g,"&apos;");
export function serializeRadarRss(articles:RadarArticle[]){ const base=getSiteUrl(); const items=articles.map(a=>{const url=new URL(`/radar/${a.slug}`,base).toString();return `<item><title>${esc(a.title)}</title><link>${esc(url)}</link><guid isPermaLink="true">${esc(url)}</guid><pubDate>${new Date(`${a.publishedAt}T12:00:00Z`).toUTCString()}</pubDate><description>${esc(a.excerpt)}</description><category>${esc(a.category)}</category></item>`}).join(""); return `<?xml version="1.0" encoding="UTF-8"?><rss version="2.0"><channel><title>Lumenva Radar</title><link>${esc(new URL("/radar",base).toString())}</link><description>${esc("IA, agentes, automação e tecnologia aplicada.")}</description><language>pt-PT</language>${items}</channel></rss>`; }
