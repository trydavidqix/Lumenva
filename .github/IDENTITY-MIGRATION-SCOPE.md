# Scope

This branch is isolated from `main`. It is dedicated to making Lumenva the only current project identity in tracked repository paths and UTF-8 content, while preserving the functional `crm_*` domain namespace.

No production cutover is performed by this branch. External infrastructure changes remain separate from repository refactoring.
